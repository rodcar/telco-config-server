server:
  port: 8889

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://rodcar:<token>@github.com/rodcar/telco-config.git
          searchPaths: '{application}'